# 위·아래 화살표가 내가 친 프롬프트를 훑는다

> 언어: [English](./en.md) · **한국어**

## 무엇이 잘못돼 있었나

입력창에서 위 화살표를 누르면 터미널에서처럼 방금 시킨 것이 다시 나와야 한다. 그런데 다시 연 대화에서는 대개 아무것도 나오지 않았다.

무언가 나올 때는 내가 친 것이 아닌 경우가 많았다. `<command-name>/model</command-name>` 같은 XML 덩어리, "[Request interrupted by user]" 표시, 대화를 압축할 때 CLI가 쓰는 요약문, CLI가 스스로 남기는 백그라운드 작업 알림이 차례로 입력창에 들어왔다.

Claude가 작업하는 동안 쳐서 큐에 들어갔던 메시지는 아예 나오지 않았다.

그리고 긴 프롬프트에서 위 화살표는 커서를 한 줄 위로 옮기지 않았다. 커서가 어디에 있든 곧장 이전 프롬프트로 건너뛰었다.

## 왜 그랬나

**히스토리를 엉뚱한 곳에서 읽고 있었다.** 채팅 화면이 들고 있는 대화 기록에서 만들었는데, 채팅 화면이 들고 있는 것은 한 페이지, 최신 **엔트리** 50개뿐이다. 엔트리는 대개 프롬프트가 아니다. 이 저장소의 대화 119개에서 사람이 친 프롬프트는 3,190개였고 그중 최신 페이지 안에 든 것은 129개, 4%였다. 가장 큰 대화는 프롬프트 193개 중 한 개도 들어 있지 않았다.

**대화 기록의 거의 전부가 `user` 엔트리다.** 9,326줄짜리 대화 하나에서 그 종류가 1,345개였고 그중 1,281개가 모델에게 되돌려주는 도구 실행 결과였다. 사람이 친 것은 46개 남짓이었다. 그것들을 전부 프롬프트로 읽은 것이 CLI가 쓴 글을 입력창에 넣은 원인이다.

**응답 중에 친 메시지는 메시지로 기록되지 않는다.** CLI는 그 텍스트를 큐에 넣었다는 것과 나중에 꺼내 썼다는 것만 기록하고, 읽는 쪽이 찾을 엔트리는 끝내 쓰지 않는다. 채팅 기록은 이미 그것을 복원하고 있었지만 히스토리는 몰랐다.

**화살표 키를 줄바꿈 문자로 판단하고 있었다.** 한 문장으로 길게 쓴 프롬프트에는 줄바꿈이 없다. 입력창이 폭에 맞춰 여러 줄로 접어 보여줄 뿐이다. 그래서 모든 줄이 "첫 줄"로 판정됐고, 문단 중간을 읽어 올라가는 중에 위 화살표가 히스토리로 넘어갔다.

## 무엇이 달라졌나

**프롬프트를 백엔드에서 받아온다.** 백엔드는 페이지가 아니라 대화 전체를 들고 있으므로, 대화를 다시 열고 위 화살표를 눌러도 아무리 오래전에 시킨 것이든 찾아준다. 한 번에 스무 개씩 오고 계속 거슬러 올라가면 이어서 받는다. 그래야 대화를 여는 비용이 싸다 — 프롬프트에는 거기 붙여넣은 것이 그대로 실려 있어서, 백 개를 한 번에 보내면 여기서 잰 최악의 대화가 21MB가 된다.

**사람이 친 것만 내놓는다.** CLI가 스스로 표시해두는 엔트리는 건너뛴다. 나머지는 이 머신의 모든 대화를 놓고 재서, 내용으로 걸러낸다 — 슬래시 명령 전개, 명령 출력, 중단 표시, CLI가 대화에 남기는 알림이 여기 해당한다.

**Claude가 작업하는 동안 친 메시지도 나온다.** 큐 기록에서 복원하며, 채팅 기록이 말풍선을 되살릴 때 쓰는 것과 **같은 규칙**을 쓴다. 그래서 화면에 보이는데 히스토리에는 없는 메시지가 생길 수 없다.

**방금 보낸 프롬프트는 곧바로 잡힌다.** CLI가 그것을 기록하기 전이어도 마찬가지다.

**화살표 키는 커서를 먼저 옮긴다.** 위 화살표는 긴 프롬프트의 줄들을 따라 올라가고, 그 다음 맨 첫 글자로 간다. 그 다음 한 번 더 눌렀을 때 — 입력창이라면 아무 일도 하지 않았을 그 누름에 — 이전 프롬프트를 불러온다. 아래 화살표는 마지막 글자에서 대칭으로 동작한다. 불러온 프롬프트는 걸어가던 방향의 끝에 커서를 놓고 도착하므로, 계속 누르면 방금 불러온 프롬프트를 되짚지 않고 다음 프롬프트로 넘어간다.

**입력창을 비우면 걷던 위치가 처음으로 돌아간다.** 그래서 다음 위 화살표는 이어서가 아니라 가장 최근 프롬프트에서 시작한다.

## 남겨둔 한계

히스토리는 지금 보고 있는 대화 안에서만 훑는다. 같은 프로젝트의 다른 대화에서 친 프롬프트는 나오지 않는다. CLI의 터미널 히스토리는 대화를 넘나들며 찾아주지만, 지금 보고 있지 않은 대화의 문장이 입력창에 튀어나오는 것은 도움보다 놀라움에 가깝다고 봤다.

슬래시 명령은 뺐다. 대화 기록에 `/model haiku`가 아니라 XML로 전개된 형태로 저장돼 있어서, 그 전개형을 돌려주는 히스토리는 아예 건너뛰는 것보다 나쁘다.
