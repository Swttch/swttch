import { readFile } from 'node:fs/promises';
import * as os from 'node:os';
import * as path from 'node:path';
import { CcbError } from '../errors.js';
function getCredentialsFilePath() {
    const configDir = process.env['CLAUDE_CONFIG_DIR'] ?? path.join(os.homedir(), '.claude');
    return path.join(configDir, '.credentials.json');
}
export async function readFileCredentials() {
    const filePath = getCredentialsFilePath();
    let raw;
    try {
        raw = await readFile(filePath, 'utf-8');
    }
    catch (err) {
        throw new CcbError(`Failed to read credentials file at ${filePath}: ${err.message}`, 'credentials_not_found', "Run 'claude login' to authenticate");
    }
    const parsed = JSON.parse(raw);
    return parsed;
}
//# sourceMappingURL=file-store.js.map