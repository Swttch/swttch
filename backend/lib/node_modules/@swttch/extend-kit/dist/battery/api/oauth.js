import { CcbError } from '../errors.js';
export class OAuthApi {
    client;
    constructor(client) {
        this.client = client;
    }
    async getUsage() {
        if (this.client.isApiKeyMode) {
            throw new CcbError('API Key authentication does not support usage/profile queries.', 'unsupported_auth', 'Check usage at console.anthropic.com, or use OAuth login (claude login)');
        }
        return this.client._request('/api/oauth/usage', {
            'anthropic-beta': 'oauth-2025-04-20',
        });
    }
    async getProfile() {
        if (this.client.isApiKeyMode) {
            throw new CcbError('API Key authentication does not support usage/profile queries.', 'unsupported_auth', 'Check usage at console.anthropic.com, or use OAuth login (claude login)');
        }
        return this.client._request('/api/oauth/profile');
    }
}
//# sourceMappingURL=oauth.js.map