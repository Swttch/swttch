import { OAuthApi } from './oauth.js';
import type { ClientAuth } from '../auth/index.js';
export declare class ClaudeCodeClient {
    private auth?;
    private accessToken?;
    private _oauth?;
    constructor(auth?: ClientAuth);
    get isApiKeyMode(): boolean;
    get oauth(): OAuthApi;
    private resolveToken;
    _request<T>(path: string, headers?: Record<string, string>): Promise<T>;
}
