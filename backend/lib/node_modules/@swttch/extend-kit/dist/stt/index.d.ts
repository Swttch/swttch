/**
 * Speech-to-text — dictation against the same service the other Claude Code
 * clients use.
 *
 * It lives out here, outside the plugin bundle, because transcribing means
 * sending the account's OAuth token and a JetBrains plugin may not handle
 * credentials. This package already reads the local Claude Code login for the
 * usage panel, so dictation reuses that path.
 */
export { openSpeechToTextStream, isSpeechToTextAvailable } from './stream.js';
export { DEFAULT_KEYTERMS, packKeyterms } from './keyterms.js';
export { AUDIO_FORMAT, TranscriptMessageType } from './types.js';
export type { SpeechToTextOptions, SpeechToTextHandlers, SpeechToTextStream, TranscriptMessage, } from './types.js';
