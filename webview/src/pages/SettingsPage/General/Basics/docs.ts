/**
 * Where the badge on a Claude-native row sends the reader.
 *
 * Shared by the rows that carry the badge so the link is written once. A
 * settings key that moves in Claude's docs then moves here, not in every row
 * that happens to mention it.
 */
export const CLAUDE_SETTINGS_DOC_HREF =
  'https://code.claude.com/docs/en/settings#available-settings';
